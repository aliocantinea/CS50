#include <cs50.h>
#include <stdio.h>

int count_letters(string text);

int main(void)
{
    string t = get_string("Text: ");

    printf("%s\n", t);

}

int count_letters(string text)
{
    for(int i = 0, l = strlen(text); i < l; i++)
    {
        
    }
}